# Desktop Vite build overrides. Release builds use safe defaults when unset.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_WEB_APP_URL=http://localhost:3000

# Optional Tauri dev-server host override.
TAURI_DEV_HOST=127.0.0.1
