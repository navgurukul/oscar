// Re-export all recording types from the shared package (single source of truth)
export {
  RecordingState,
} from "@oscar/shared/types";

export type {
  TranscriptUpdate,
  RecordingConfig,
  RecordingError,
  RecordingSession,
} from "@oscar/shared/types";
